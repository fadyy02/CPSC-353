// vigenere.h
#ifndef VIGENERE_H
#define VIGENERE_H

#include <string>

using namespace std;

string vigenereEncrypt(string text, string key);
string vigenereDecrypt(string text, string key);

#endif
